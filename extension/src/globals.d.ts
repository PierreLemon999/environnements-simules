/** Injected by Vite at build time from manifest.json version */
declare const __APP_VERSION__: string;
/** Short alphanumeric hash that changes on every build (for dev traceability) */
declare const __BUILD_HASH__: string;
